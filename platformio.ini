; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:seeed_xiao_esp32s3]
platform = espressif32@7.0.1
framework = espidf
board = seeed_xiao_esp32s3
; ESP-IDF 6.0 / PlatformIO: builder erroneously appends the C++-only
; -fuse-cxa-atexit flag to C compilations, causing -Werror to fail.
build_unflags = -fuse-cxa-atexit
monitor_speed = 115200
monitor_raw = yes
; dtr & rts set to 0 on monitor activation will reset chip
monitor_dtr = 0
monitor_rts = 0
; Adds the ESPHome: * targets. ESPHome builds via its own CMake/ninja project,
; so these shell out to the ESPHome CLI rather than using PlatformIO's builder.
extra_scripts = post:scripts/esphome_targets.py
